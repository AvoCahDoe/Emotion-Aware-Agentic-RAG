type: faq
# Supported file types

NovaAssist indexes Markdown (.md), plain text (.txt), and PDF text layers. Scanned image-only PDFs are not supported unless you OCR them first. Maximum file size is 2 MB on Free and 10 MB on Pro. Binary formats like .docx are not supported yet — export to Markdown first.
